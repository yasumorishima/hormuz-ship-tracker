# Sentinel-1 collection (.github/workflows/sar-collect.yml).
#
# rasterio brings its own GDAL in the wheel, so nothing is installed from apt
# and the job does not depend on the runner image's raster stack.
rasterio>=1.3
numpy>=1.26
scipy>=1.11
pyarrow>=16.0
huggingface_hub>=0.25
