# tests.yml. The parser and backfill tests need nothing; the window-loop test
# stands up a local WebSocket server, and the last step parses the workflows.
# The exact version is load-bearing: the one-argument `async def handler(ws)`
# in tests/test_window_collect.py is the modern asyncio signature. Do not
# relax this to >=.
websockets==15.0.1
pyyaml>=6.0
